import React from 'react'

function Contact(){
    return(
        <div>
            <h2>Contact</h2>
            {console.log('changed to Contact page')}
        </div>
    );
}

export default Contact